<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Normal User Dashboard - NTPC Safety Management</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <div class="flex justify-between items-center mb-4">
            <h1 class="text-2xl font-bold">Welcome, <%= user.name %> (Normal User)</h1>
            <button onclick="confirmLogout()" class="bg-red-500 text-white p-2 rounded-md hover:bg-red-600">Logout</button>
        </div>
        <% if (successMessage) { %>
            <p class="text-green-500 mb-4"><%= successMessage %></p>
        <% } %>
        <% if (errorMessage) { %>
            <p class="text-red-500 mb-4"><%= errorMessage %></p>
        <% } %>
        <a href="/" class="bg-blue-500 text-white p-2 rounded-md mb-4 inline-block">Record New Observation</a>
        <h2 class="text-xl font-semibold mb-2">Your Observations</h2>
        <table class="w-full border">
            <thead>
                <tr class="bg-gray-200">
                    <th class="border p-2">Description</th>
                    <th class="border p-2">Status</th>
                </tr>
            </thead>
            <tbody>
                <% observations.forEach(obs => { %>
                    <tr>
                        <td class="border p-2"><%= obs.description %></td>
                        <td class="border p-2"><%= obs.status %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <script>
        function confirmLogout() {
            if (confirm('Are you sure you want to logout?')) {
                window.location.href = '/auth/logout';
            }
        }
    </script>
</body>
</html>