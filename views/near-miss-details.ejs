<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Near Miss Details - NTPC Safety Portal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f4f7fa;
    }
    .card {
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
  <%- include('partials/navbar') %>

  <div class="container my-5">
    <div class="card p-4">
      <h1 class="card-title mb-4">Near Miss Incident Details</h1>
      <% if (successMessage) { %>
        <div class="alert alert-success" role="alert"><%= successMessage %></div>
      <% } %>
      <% if (errorMessage) { %>
        <div class="alert alert-danger" role="alert"><%= errorMessage %></div>
      <% } %>
      <div class="mb-3">
        <strong>Reported By:</strong> <%= nearMiss.userId.name %>
      </div>
      <div class="mb-3">
        <strong>Incident Date:</strong> <%= nearMiss.incidentDate.toLocaleString() %>
      </div>
      <div class="mb-3">
        <strong>Incident Zone:</strong> <%= nearMiss.incidentZone %>
      </div>
      <div class="mb-3">
        <strong>Incident Location:</strong> <%= nearMiss.incidentLocation %>
      </div>
      <div class="mb-3">
        <strong>Details:</strong>
        <p><%= nearMiss.incidentDetails %></p>
      </div>
      <% if (nearMiss.uploadedFileUrl) { %>
        <div class="mb-3">
          <strong>Uploaded File:</strong>
          <a href="<%= nearMiss.uploadedFileUrl %>" target="_blank" class="btn btn-sm btn-primary">View File</a>
        </div>
      <% } %>
      <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
    </div>
  </div>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>