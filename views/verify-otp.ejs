<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verify OTP - NTPC Safety Portal</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
        min-height: 100vh;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .otp-card {
        background: white;
        border-radius: 16px;
        padding: 3rem;
        max-width: 450px;
        width: 100%;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        position: relative;
        animation: fadeIn 0.6s ease-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      .otp-card h2 {
        font-weight: 700;
        font-size: 2rem;
        margin-bottom: 0.5rem;
        color: #1e3a8a;
        text-align: center;
      }

      .otp-card p {
        color: #6b7280;
        text-align: center;
        margin-bottom: 2rem;
      }

      .form-control {
        padding: 0.75rem 1rem;
        font-size: 1.2rem;
        border-radius: 10px;
        text-align: center;
        letter-spacing: 8px;
      }

      .btn-verify {
        margin-top: 1.5rem;
        background: #1e3a8a;
        color: white;
        padding: 0.75rem;
        border-radius: 10px;
        border: none;
        width: 100%;
        font-size: 1rem;
        font-weight: 600;
        transition: 0.3s ease;
      }

      .btn-verify:hover {
        background: #3b82f6;
      }

      .resend-link {
        text-align: center;
        margin-top: 1.5rem;
        font-size: 0.9rem;
      }

      .resend-link a {
        color: #3b82f6;
        font-weight: 600;
        text-decoration: none;
      }

      .resend-link a:hover {
        text-decoration: underline;
      }

      .alert-danger {
        background: #fee2e2;
        border: 1px solid #fca5a5;
        color: #b91c1c;
        padding: 1rem;
        border-radius: 10px;
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <div class="otp-card">
      <h2>Verify OTP</h2>
      <p>Enter the 6-digit code sent to your email</p>

      <% if (errorMessage) { %>
      <div class="alert alert-danger"><%= errorMessage %></div>
      <% } %>

      <form action="/auth/verify-otp" method="POST">
        <!-- Hidden input to retain email -->
        <input type="hidden" name="email" value="<%= email %>" />

        <!-- OTP input field -->
        <label for="otp">Enter OTP:</label>
        <input type="text" name="otp" id="otp" required />

        <button type="submit">Verify OTP</button>

        <% if (errorMessage) { %>
        <div class="alert alert-danger"><%= errorMessage %></div>
        <% } %>
      </form>

      <div class="resend-link">
        Didn't get the code? <a href="/auth/resend-otp">Resend OTP</a>
      </div>
    </div>
  </body>
</html>
